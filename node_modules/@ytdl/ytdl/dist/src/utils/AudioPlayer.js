"use strict";
/* eslint global-require: "off" */
/* eslint import/no-extraneous-dependencies: "off" */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
exports.__esModule = true;
var fluent_ffmpeg_1 = __importDefault(require("fluent-ffmpeg"));
var AudioPlayer = /** @class */ (function () {
    function AudioPlayer() {
        this.url = '';
        this.player = 'ytdl-mp3';
        this.Speaker = require('speaker');
    }
    /**
     * Play video given a url and a stream.
     * @param url Stream URL
     * @param stream Media stream
     */
    AudioPlayer.prototype.play = function (url, stream) {
        this.url = url;
        this.stream = stream;
        fluent_ffmpeg_1["default"](stream).toFormat('s16le').pipe(new this.Speaker());
    };
    return AudioPlayer;
}());
exports["default"] = AudioPlayer;
